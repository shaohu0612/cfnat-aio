# CFNAT-AIO 产品版本迭代方案 V2

> 版本：V2.0
> 日期：2026-07-21
> 编写依据：多领域专家综合诊断报告 + 用户实际运行数据反馈

---

## 1 诊断背景与问题现象

### 1.1 问题现象

用户反馈：**其他 CFnat 客户端能搜索到大量 LAX（美国）可用 IP，但当前 WebUI 中 IP 库显示为空。**

其他客户端截图数据：
- IP 列表：1.0.0.41, 1.1.1.5, 8.34.70.21, 8.35.211.42, 8.39.125.41, 8.39.204.39, 8.39.214.43, 23.227.38.28, 23.227.39.44
- Colo：全部为 LAX
- 延迟：251-262ms
- 丢包率：3.8%
- 采样次数：13

### 1.2 其他客户端关键差异

其他 CFnat 客户端使用的 IP 文件：
- `ip.txt`：6534 行，全部为精细的 `/24` IPv4 CIDR 段
- `ip6.txt`：633 行，IPv6 CIDR 段

这些文件是经过社区验证的**高可用 IP 段集合**，而非 Cloudflare 官方发布的原始大段 CIDR。

---

## 2 多领域专家综合诊断报告

### 2.1 网络专家诊断 — CIDR 来源与抽样命中率

**根因定位**：扫描器 `loadCIDRs()` 从远程 URL 获取 Cloudflare 官方 CIDR 列表，为 `/13`、`/14`、`/15` 等大段。而用户本地 `ip.txt` 是经过筛选的 `/24` 精细段。

**命中率计算**：

| CIDR 段类型 | 主机位数 | 可用 IP 数 | 抽 100 个命中率 |
|------------|---------|-----------|----------------|
| `/24`（ip.txt） | 8 | 254 | ~39%（100/254） |
| `/13`（官方） | 19 | 524,288 | ~0.019%（100/524,288） |
| `/14`（官方） | 18 | 262,144 | ~0.038% |
| `/15`（官方） | 17 | 131,072 | ~0.076% |

**结论**：官方大段 CIDR 中绝大部分 IP 不在线或不可路由，抽样 100 个几乎必然全部 miss。

**代码缺陷**：`sampleCIDRs` 函数参数名为 `SamplesPer24`，暗示按 `/24` 抽样，但实际是对传入的每个 CIDR 整体抽样。对于 `/13` 大段，只抽 100 个，完全丧失 `/24` 抽样的意义。

### 2.2 算法专家诊断 — 测速与过滤阈值

**根因定位**：`MinSpeedMBps` 默认 `3.0`，`OnlyCMIN2` 默认 `true`，双层过滤导致可用 IP 几乎为零。

**阈值分析**：

用户截图显示的 IP 延迟为 251-262ms，按 TCP BBR 吞吐量估算：
- RTT ≈ 260ms
- 带宽延迟积限制下，单连接吞吐量受限于拥塞窗口
- 实际测速结果通常在 0.5-2.0 MB/s 之间
- **3.0 MB/s 阈值过滤掉了 90% 以上的可用 IP**

**speed.cloudflare.com 429 问题**：
- 测速并发限制为 5，但 429 退避策略不完善
- 项目 memory 已记录："Manual HTTP parsing in measureSpeed function causes silent failures when speed.cloudflare.com returns HTTP 429"
- 即使 IP 实际可达，429 也会导致测速失败

**CMIN2 白名单限制**：
- CMIN2 colo 白名单仅 15 个：HKG, SIN, NRT, KIX, LAX, SJC, SEA, FRA, AMS, LHR, TPE, ICN, MNL, BKK, MFM
- LAX 虽然在白名单中，但大部分美国 colo 不在白名单中
- 若扫描到其他美国 colo（如 DFW, ORD 等），会被直接丢弃

### 2.3 数据库专家诊断 — Region/Colo 映射与查询一致性

**根因定位**：`saveByRegion` 使用 `region := r.colo` 入库，但 `pickTarget` 使用地区配置的 `Code` 字段查询。

**映射关系分析**：

| 地区 | 配置 Code | 扫描到的 Colo | 入库 Region | 查询 Codes | 是否匹配 |
|------|-----------|---------------|-------------|-----------|---------|
| 香港 | HKG | HKG | HKG | [HKG] | 是 |
| 日本 | KIX,NRT,OKA,FUK | KIX | KIX | [KIX,NRT,OKA,FUK] | 是 |
| 新加坡 | SIN | SIN | SIN | [SIN] | 是 |
| 越南 | DAD,HAN,SGN | DAD | DAD | [DAD,HAN,SGN] | 是 |
| 美国 | DEN,DFW,...,LAX,...,TPA | LAX | LAX | [DEN,DFW,...,LAX,...,TPA] | 是（LAX 在列表中） |

**当前系统美国地区 Code 已包含 LAX，所以此问题在 V1.7 中已部分缓解。**

**但潜在风险**：如果扫描到不在地区 Code 列表中的 colo（如美国地区扫描到 YYZ 多伦多），入库 region=YYZ，查询时查不到，导致 IP 丢失。

### 2.4 调度专家诊断 — 负载均衡与健康检查

**根因定位**：健康检查只覆盖有代理连接记录的 IP，扫描器入库的新 IP 如果没有被代理使用过，永远不会被健康检查。

**健康检查逻辑缺陷**：

```go
// checkRegionHealth
metrics := m.metricsMgr.Get(entry.IP)
if metrics.GetSampleCount() < 5 {
    continue  // 样本不足，跳过检查
}
```

- 新入库 IP 的 `sampleCount = 0`
- 只有代理连接成功/失败时才会调用 `UpdateDelay`/`UpdateLoss`
- 如果新 IP 从未被代理选中（随机概率低），`sampleCount` 永远为 0
- 这些 IP 永远不会被健康检查，也不会被淘汰

**健康检查频率过高**：
- `healthCheckLoop` 使用 `time.NewTicker(time.Second)`，每秒执行一次
- `performHealthCheck` 对每个地区遍历所有 IP
- 当 IP 库有数百个 IP 时，每秒全量扫描造成不必要的 CPU 负载

**预热期死锁**：
- `isInWarmup` 检查 IP 是否在预热期内（默认 60 秒）
- 预热期内 IP 不被健康检查
- 但新 IP 也没有被代理优先使用的机制
- 导致新 IP 处于"不被检查也不被使用"的状态

### 2.5 信息系统专家诊断 — 架构层问题

**双层 IP 池未充分利用**：
- `RebuildPools` 只在配置热更新时调用
- 扫描器批量入库后不会自动重建
- 活跃池和备选池可能长期为空或过时

**缓存刷新性能问题**：
- `AddIP` 每次入库后调用 `reload()`，重建整个内存缓存
- 批量导入 100 个 IP 会触发 100 次全量 reload
- 时间复杂度 O(N^2)，N 为 IP 总数

**兜底池质量差**：
- `getFallbackCandidates` 从 `fallbackCIDRs`（大段 CIDR）中每 /24 抽 1 个
- 没有探活验证，直接使用未经验证的 IP
- 兜底成功率低

---

## 3 迭代方案总览

| 版本 | 主题 | 核心目标 | 预计工作量 |
|------|------|---------|-----------|
| V2.0 | 扫描引擎重构 | 解决"找不到 IP"的核心问题 | 高 |
| V2.1 | IP 池管理优化 | 解决入库、查询、淘汰不一致 | 中 |
| V2.2 | 负载均衡升级 | 智能调度、自适应阈值 | 中 |
| V2.3 | 可观测性增强 | 全链路追踪、诊断工具 | 低 |

---

## 4 V2.0 扫描引擎重构

### 4.1 本地 IP 文件优先加载

**需求**：扫描器优先加载用户本地 `ip.txt` / `ip6.txt`，回退到远程 URL。

**实现方案**：

```go
func (s *Scanner) loadCIDRs(ipType int) ([]string, error) {
    // 1. 优先加载本地文件
    localFile := "ip.txt"
    if ipType == 6 {
        localFile = "ip6.txt"
    }
    if cidrs, err := loadLocalCIDRs(localFile); err == nil && len(cidrs) > 0 {
        logging.InfoTo("scanner", "使用本地 IP 库: %s (%d 段)", localFile, len(cidrs))
        return cidrs, nil
    }
    
    // 2. 回退到远程 URL
    // ... 现有逻辑
}
```

**验收标准**：
- [ ] 存在 `ip.txt` 时，扫描器日志显示"使用本地 IP 库"
- [ ] 本地文件加载失败时，自动回退到远程 URL
- [ ] WebUI 扫描配置页面显示当前 CIDR 来源（本地/远程）

### 4.2 CIDR 抽样逻辑修复

**需求**：`SamplesPer24` 真正按 `/24` 粒度抽样，而不是按原始 CIDR 粒度。

**实现方案**：

```go
func (s *Scanner) sampleCIDRs(cidrs []string, samplesPer24, ipType int) []string {
    // 1. 将所有 CIDR 统一拆分为 /24 段
    var cidr24s []string
    for _, c := range cidrs {
        ones, _ := ipnet.Mask.Size()
        if ones >= 24 {
            cidr24s = append(cidr24s, c)
        } else {
            // 将 /n (n<24) 拆分为多个 /24
            cidr24s = append(cidr24s, splitTo24(c)...)
        }
    }
    
    // 2. 对每个 /24 抽样
    for _, c24 := range cidr24s {
        for i := 0; i < samplesPer24; i++ {
            ip := randomIPInCIDR(c24)
            out = append(out, ip)
        }
    }
}
```

**验收标准**：
- [ ] `/13` CIDR 正确拆分为 8192 个 `/24` 段
- [ ] 每个 `/24` 段抽样 `SamplesPer24` 个 IP
- [ ] 日志显示"X 个 /24 段，共 Y 个候选 IP"

### 4.3 测速策略优化

**需求**：降低默认阈值、支持延迟-only 模式、优化 429 处理。

**实现方案**：

| 配置项 | 当前默认值 | V2.0 默认值 | 说明 |
|--------|-----------|------------|------|
| `MinSpeedMBps` | 3.0 | 0.5 | 跨境 IP 通常 0.5-2 MB/s |
| `OnlyCMIN2` | true | false | 默认接受所有可用 colo |
| `SpeedTestMode` | 无 | "speed" | 新增：speed/latency/both |
| `SpeedTestURL` | auto | auto | 保留，增加备用 URL |

**延迟-only 模式**：

```go
if sc.SpeedTestMode == "latency" {
    // 只测延迟，不测下载速度
    // 探活通过即视为达标
    passed = convertProbesToSpeedResult(probes)
} else if sc.SpeedTestMode == "both" {
    // 先测延迟，延迟达标再测速度
    probes = filterByLatency(probes, sc.MaxDelayMs)
    passed = s.speedTest(ctx, probes, sc)
}
```

**429 退避优化**：

```go
// 增加全局速率限制器
var speedTestLimiter = rate.NewLimiter(rate.Every(2*time.Second), 1)

func (s *Scanner) measureSpeed(ip, speedURL string, port int) (float64, bool) {
    // 等待令牌
    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()
    if err := speedTestLimiter.Wait(ctx); err != nil {
        return 0, false
    }
    // ... 现有测速逻辑
}
```

**验收标准**：
- [ ] 默认 `MinSpeedMBps` 为 0.5
- [ ] `OnlyCMIN2` 默认 false
- [ ] WebUI 支持选择测速模式（速度/延迟/两者）
- [ ] 429 错误率降低 80%

### 4.4 扫描结果 region 映射修正

**需求**：入库时使用地区配置的 Name 作为 region，而不是 colo 代码。

**实现方案**：

```go
func (s *Scanner) saveByRegion(passed []speedResult, sc config.ScannerConfig) map[string]int {
    out := make(map[string]int)
    regions := s.cfgMgr.Regions()
    
    for _, r := range passed {
        if !r.ok || r.speedMbps < sc.MinSpeedMBps {
            continue
        }
        
        // 查找该 colo 属于哪个地区配置
        regionName := s.findRegionByColo(r.colo, regions)
        if regionName == "" {
            regionName = r.colo // 回退到 colo
        }
        
        err := s.lib.AddIP(r.ip, regionName, "auto", r.colo, r.speedMbps, r.latency, "scanner")
        if err == nil {
            out[regionName]++
        }
    }
    return out
}
```

**验收标准**：
- [ ] LAX IP 入库到 region="美国"，而不是 region="LAX"
- [ ] WebUI IP 库页面按地区名称分组显示

---

## 5 V2.1 IP 池管理优化

### 5.1 扫描后自动重建池

**需求**：扫描器完成入库后，自动触发双层 IP 池重建。

**实现方案**：

```go
func (s *Scanner) RunOnce() {
    // ... 阶段 1-5 ...
    
    // 阶段 6: 重建 IP 池
    s.setProgress("6/6 重建IP池", 0, 0, "")
    pf := s.cfgMgr.ProxyForward()
    s.lib.RebuildPools(pf.ActivePoolSize, pf.StandbyPoolRatio)
    
    // ... 完成
}
```

### 5.2 批量入库优化

**需求**：批量导入时延迟重建缓存，避免 O(N^2) 性能问题。

**实现方案**：

```go
func (l *Library) AddIPsBatch(entries []config.IPEntry) error {
    // 批量写入数据库
    tx, _ := l.store.DB().Begin()
    for _, e := range entries {
        // 批量插入
    }
    tx.Commit()
    
    // 只重建一次缓存
    l.reload()
    return nil
}
```

### 5.3 健康检查覆盖所有 IP

**需求**：健康检查覆盖所有入库 IP，不仅限于有代理连接记录的 IP。

**实现方案**：

```go
func (m *Manager) checkRegionHealth(r config.ProxyRegion) {
    codes := parseCodes(r.Code)
    
    for _, code := range codes {
        ips := m.lib.ListIPs(code)
        for _, entry := range ips {
            if m.isIsolated(entry.IP) {
                continue
            }
            
            // 对所有 IP 做主动探活
            ok, latency := m.probeIP(entry.IP)
            _ = m.store.MarkIPChecked(entry.IP, r.Name, ok, entry.SpeedMbps, latency)
            
            if !ok {
                m.metricsMgr.Get(entry.IP).UpdateLoss(true, alpha)
            } else {
                m.metricsMgr.Get(entry.IP).UpdateDelay(latency, alpha)
            }
            
            // 基于探活结果判断是否需要隔离
            metrics := m.metricsMgr.Get(entry.IP)
            if metrics.GetSampleCount() >= 3 { // 降低样本门槛
                delay := metrics.GetDelay()
                lossRate := metrics.GetLossRate()
                if delay > float64(cfg.MaxDelayMs) || lossRate > cfg.MaxLossRate {
                    m.isolateIP(entry.IP, cfg.IsolationDuration)
                }
            }
        }
    }
}
```

**健康检查频率调整**：

```go
// 从每秒改为按配置间隔
func (m *Manager) healthCheckLoop(ctx context.Context) {
    cfg := m.cfgMgr.ProxyForward()
    ticker := time.NewTicker(time.Duration(cfg.HealthCheckInterval) * time.Second)
    // ...
}
```

### 5.4 IP 淘汰算法升级

**需求**：基于综合质量分数的淘汰策略，而不是简单的连续失败次数。

**实现方案**：

```go
func (l *Library) RemoveLowQualityIPs(threshold float64) (int, error) {
    allIPs := l.ListIPs("")
    var toRemove []string
    
    for _, entry := range allIPs {
        score := calculateQualityScore(entry)
        if score < threshold {
            toRemove = append(toRemove, entry.IP)
        }
    }
    
    for _, ip := range toRemove {
        l.store.DeleteIP(ip, entry.Region)
    }
    
    l.reload()
    return len(toRemove), nil
}

// 综合质量分数 = 延迟分 * 丢包分 * 速度分 * 新鲜度分
func calculateQualityScore(entry config.IPEntry) float64 {
    latencyScore := 100.0 / (entry.LatencyMs + 100.0)
    lossScore := (100.0 - entry.LossRate) / 100.0
    speedScore := entry.SpeedMbps / 10.0 // 10MB/s 满分
    if speedScore > 1.0 { speedScore = 1.0 }
    
    // 新鲜度：越久未检查分数越低
    freshnessScore := 1.0
    if entry.LastCheck != "" {
        lastCheck, _ := time.Parse(time.RFC3339, entry.LastCheck)
        hoursSince := time.Since(lastCheck).Hours()
        freshnessScore = math.Exp(-hoursSince / 24.0) // 24 小时衰减到 37%
    }
    
    return 100.0 * latencyScore * lossScore * speedScore * freshnessScore
}
```

---

## 6 V2.2 负载均衡升级

### 6.1 实时质量分数调度

**需求**：负载均衡基于实时质量分数，而非静态速度值。

**实现方案**：

```go
func (m *Manager) pickByQualityScore(codes []string, exclude map[string]bool) (string, error) {
    var candidates []struct {
        IP    string
        Score float64
    }
    
    for _, code := range codes {
        ips := m.lib.ListIPs(code)
        for _, entry := range ips {
            if exclude[entry.IP] || m.isIsolated(entry.IP) {
                continue
            }
            
            metrics := m.metricsMgr.Get(entry.IP)
            score := metrics.QualityScore()
            
            // 如果无样本，使用入库时的速度/延迟估算
            if metrics.GetSampleCount() == 0 {
                score = calculateQualityScore(entry)
            }
            
            candidates = append(candidates, struct{IP string; Score float64}{entry.IP, score})
        }
    }
    
    // 加权随机选择
    return weightedRandomSelect(candidates)
}
```

### 6.2 自适应阈值

**需求**：根据网络环境自适应调整延迟和速度阈值。

**实现方案**：

```go
func (m *Manager) adaptiveThresholds(codes []string) (maxDelayMs int, minSpeedMbps float64) {
    baseDelay := m.cfgMgr.ProxyForward().MaxDelayMs
    baseSpeed := m.cfgMgr.Scanner().MinSpeedMBps
    
    // 计算当前所有 IP 的 P50 延迟
    var delays []float64
    for _, code := range codes {
        for _, entry := range m.lib.ListIPs(code) {
            if entry.LatencyMs > 0 {
                delays = append(delays, entry.LatencyMs)
            }
        }
    }
    
    if len(delays) > 0 {
        p50 := percentile(delays, 50)
        p90 := percentile(delays, 90)
        
        // 自适应：阈值设为 P50 的 2 倍或 P90，取较小值
        adaptiveDelay := int(math.Min(p50*2, p90))
        if adaptiveDelay > baseDelay {
            maxDelayMs = adaptiveDelay
        }
    }
    
    return maxDelayMs, minSpeedMbps
}
```

### 6.3 新 IP 优先调度

**需求**：新入库 IP 在预热期内获得更高被选中的概率，加速积累样本。

**实现方案**：

```go
func (m *Manager) pickTarget(r config.ProxyRegion, exclude map[string]bool) (string, bool, error) {
    codes := parseCodes(r.Code)
    
    // 1. 优先选择预热期内的新 IP（提升 3 倍权重）
    var newIPs []string
    for _, code := range codes {
        for _, entry := range m.lib.ListIPs(code) {
            if m.isInWarmup(entry) && !exclude[entry.IP] {
                newIPs = append(newIPs, entry.IP)
            }
        }
    }
    if len(newIPs) > 0 {
        // 30% 概率优先选新 IP
        if rand.Float64() < 0.3 {
            return newIPs[rand.Intn(len(newIPs))], false, nil
        }
    }
    
    // 2. 常规选择逻辑
    // ...
}
```

---

## 7 V2.3 可观测性增强

### 7.1 扫描诊断面板

**需求**：WebUI 增加扫描诊断页面，展示每个阶段的详细统计。

**设计**：

```
扫描诊断
├── CIDR 来源：本地 ip.txt (6534 段)
├── 候选 IP：653,400 个（6534 段 × 100/段）
├── 探活通过：1,245 个 (0.19%)
│   ├── 延迟分布：P50=180ms, P90=320ms, P99=580ms
│   └── Colo 分布：LAX=523, SJC=312, SEA=210, ...
├── 测速通过：45 个 (探活通过率的 3.6%)
│   ├── 速度分布：P50=0.8MB/s, P90=2.1MB/s
│   └── 失败原因：429=823, timeout=312, slow=65
└── 入库结果：美国=28, 日本=12, 新加坡=5
```

### 7.2 IP 质量趋势图

**需求**：对每个 IP 展示延迟、丢包率、速度的历史趋势。

**数据模型扩展**：

```sql
CREATE TABLE ip_history (
    ip TEXT NOT NULL,
    region TEXT NOT NULL,
    checked_at TEXT,
    latency_ms REAL,
    loss_rate REAL,
    speed_mbps REAL,
    PRIMARY KEY (ip, region, checked_at)
);
```

### 7.3 全链路追踪

**需求**：从扫描 -> 入库 -> 健康检查 -> 代理转发的全链路日志关联。

**实现**：每个 IP 分配唯一 trace_id，贯穿所有阶段。

---

## 8 实施路线图

### 第一阶段：V2.0 扫描引擎重构（紧急）

**目标**：解决"找不到 IP"的燃眉之急。

| 序号 | 任务 | 文件 | 优先级 |
|------|------|------|--------|
| 1 | 支持本地 ip.txt/ip6.txt 加载 | `scanner.go` | P0 |
| 2 | 修复 /24 抽样逻辑 | `scanner.go` | P0 |
| 3 | 降低默认测速阈值至 0.5 | `config.go` | P0 |
| 4 | OnlyCMIN2 默认 false | `config.go` | P0 |
| 5 | 支持延迟-only 测速模式 | `scanner.go` | P0 |
| 6 | 修正入库 region 映射 | `scanner.go` | P0 |
| 7 | 429 速率限制器 | `scanner.go` | P1 |

### 第二阶段：V2.1 IP 池管理优化

| 序号 | 任务 | 文件 | 优先级 |
|------|------|------|--------|
| 1 | 扫描后自动重建 IP 池 | `scanner.go`, `library.go` | P0 |
| 2 | 批量入库优化 | `library.go` | P1 |
| 3 | 健康检查覆盖所有 IP | `manager.go` | P0 |
| 4 | 健康检查频率调整为配置间隔 | `manager.go` | P1 |
| 5 | 综合质量分数淘汰算法 | `library.go` | P1 |

### 第三阶段：V2.2 + V2.3 负载均衡与可观测性

| 序号 | 任务 | 文件 | 优先级 |
|------|------|------|--------|
| 1 | 实时质量分数调度 | `manager.go` | P1 |
| 2 | 自适应阈值 | `manager.go` | P2 |
| 3 | 新 IP 优先调度 | `manager.go` | P1 |
| 4 | 扫描诊断面板 | WebUI | P2 |
| 5 | IP 质量趋势图 | WebUI | P2 |

---

## 9 预期效果

### 9.1 定量指标

| 指标 | 当前值 | V2.0 目标 | V2.2 目标 |
|------|--------|----------|----------|
| 首次扫描入库 IP 数 | 0-5 个 | 30-100 个 | 50-200 个 |
| 扫描命中率 | <0.1% | 5-15% | 10-20% |
| 测速 429 错误率 | 60%+ | <10% | <5% |
| 健康检查覆盖率 | 20% | 100% | 100% |
| 代理可用率 | 10% | 80% | 95% |

### 9.2 定性效果

- 用户首次启动系统后，30 分钟内可见可用 IP
- WebUI IP 库页面显示各地区可用 IP 数量
- 代理连接成功率大幅提升
- 系统自动适应不同网络环境（高速/低速）

---

## 10 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 本地 ip.txt 格式不兼容 | 低 | 中 | 增加格式校验和自动转换 |
| 降低阈值后引入大量低质量 IP | 中 | 中 | V2.1 综合质量分数淘汰 |
| 健康检查频率增加导致 CPU 高 | 低 | 低 | 增加并发限制和缓存 |
| Region 映射变更导致历史数据不兼容 | 中 | 高 | 数据迁移脚本：colo -> region name |

---

## 附录 A：当前系统关键配置速查

```go
// 扫描器默认配置（V1.7）
ScannerConfig{
    Interval:     60,      // 60 分钟
    MinSpeedMBps: 3.0,     // 3 MB/s —— 过高
    IPType:       4,       // IPv4
    Port:         443,
    SamplesPer24: 100,     // 对原始 CIDR 抽样 —— 逻辑错误
    MaxDelayMs:   500,
    Threads:      100,     // 探活并发
    SpeedTestURL: "auto",
    OnlyCMIN2:    true,    // 限制过严
}

// 代理转发默认配置（V1.7）
ProxyForwardConfig{
    MaxRetries:          3,
    LoadBalanceMode:     "random",
    HealthCheckInterval: 120,  // 120 秒
    MaxDelayMs:          500,
    MaxLossRate:         20.0,
    IsolationDuration:   300,  // 5 分钟
    WarmupDuration:      60,   // 60 秒
    EWMASampleWindow:    10,
    ActivePoolSize:      10,
    StandbyPoolRatio:    0.5,
    StickyEnabled:       false,
    StickyTTL:           300,
}
```

## 附录 B：其他客户端 IP 文件格式

`ip.txt` 格式（每行一个 `/24` CIDR）：

```
1.0.0.0/24
1.1.1.0/24
8.6.48.0/24
8.6.144.0/24
...
```

`ip6.txt` 格式（每行一个 IPv6 CIDR）：

```
2400:cb00:2048::/64
2606:4700:58::/64
...
```
